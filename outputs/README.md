# Outputs

The root `outputs/` directory intentionally contains no current verdict file.
The former `verdict.json` and `verify_run.log` belonged to the rejected 6×6
toy/proxy verifier and were removed from the current publication surface.

Authoritative claim evidence is versioned under
[`release/space_candidate/evidence`](../release/space_candidate/evidence), with
the current summary at
[`release/space_candidate/pages/current-verification/page.md`](../release/space_candidate/pages/current-verification/page.md).

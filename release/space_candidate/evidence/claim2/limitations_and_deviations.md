# Claim 2 limitations and deviations

No numerical or algorithmic substitution is made: the verifier uses the
printed degree-one recurrence and exact optimization interval. The matrix is
small because it is a proof certificate, not a scaling experiment; exact
rational arithmetic exhausts every quantity needed for the contradiction.

The result is scoped to the theorem exactly as printed. If the intended theorem
also assumed `A^2` positive semidefinite (or an equivalent real-spectrum
condition), this counterexample would lie outside that repaired domain. That
condition is not present in `Thmtheorem1`.

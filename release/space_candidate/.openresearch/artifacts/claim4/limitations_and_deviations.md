# Limitations and deviations

GPU use is forbidden by the campaign. More importantly, the paper does not
publish enough operational detail to instantiate the exact two runs. The CPU
models omit Shampoo and the unpublished channel enlargement; they are never
used as a proxy verdict. Missing evidence is not falsification.

# Claim 1 evaluator status

Status before the run: **BLOCKED pending evidence**.

The committed verifier is faithful to the named fifth-order PRISM mechanism,
uses the paper dimensions and distributions, audits the global alpha fit, and
has an adaptation control. The claim becomes eligible for `VERIFIED` only
after the fixed command produces a complete raw result and an independent
dense checker confirms the spectral-coordinate implementation.
